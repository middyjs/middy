{
  "name": "middy-monorepo",
  "version": "2.0.0-alpha.0",
  "description": "🛵 The stylish Node.js middleware engine for AWS Lambda",
  "type": "module",
  "engines": {
    "node": ">=14"
  },
  "engineStrict": true,
  "scripts": {
    "test:package": "c8 ava packages/validator/__tests__/index.js",
    "install": "lerna bootstrap",
    "test": "npm run test:lint && npm run test:packages",
    "test:lint": "standard --fix",
    "test:lint:ci": "standard",
    "test:packages": "npm run test:packages:typings && npm run test:packages:unit",
    "test:packages:unit": "c8 lerna exec --bail --concurrency 5 npm run test:unit",
    "test:packages:typings": "lerna exec --bail --concurrency 5 npm run test:typings",
    "release:tag": "git tag $npm_package_version && git push --tags",
    "lerna:rm": "npm run lerna:rm:node_modules && npm run lerna:rm:build",
    "lerna:rm:build": "lerna exec -- rm -rf index.cjs",
    "lerna:rm:node_modules": "lerna exec -- rm -rf node_modules",
    "lerna:install": "lerna exec --bail --concurrency 5 npm i",
    "lerna:update": "lerna exec --bail --concurrency 5 npm update",
    "lerna:outdated": "lerna exec --concurrency 2 npm outdated",
    "lerna:audit": "lerna exec --concurrency 2 npm audit fix",
    "lerna:sync": "lerna exec --bail --concurrency 2 npm install && lerna publish --yes --skip-npm --skip-git --repo-version $npm_package_version",
    "lerna:build": "lerna exec --bail --concurrency 2 -- babel  index.js --out-file index.cjs",
    "lerna:publish": "lerna publish --yes --skip-git --repo-version $npm_package_version"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/middyjs/middy.git"
  },
  "keywords": [
    "Lambda",
    "Middleware",
    "Serverless",
    "Framework",
    "AWS",
    "AWS Lambda"
  ],
  "author": {
    "name": "Middy contributors",
    "url": "https://github.com/middyjs/middy/graphs/contributors"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/middyjs/middy/issues"
  },
  "homepage": "https://middy.js.org",
  "devDependencies": {
    "@babel/cli": "7.12.10",
    "@babel/core": "7.12.10",
    "@babel/preset-env": "7.12.11",
    "ava": "^3.14.0",
    "c8": "7.4.0",
    "eslint-plugin-import": "^2.22.1",
    "husky": "^4.3.6",
    "lerna": "^3.22.1",
    "lint-staged": "^10.5.3",
    "sinon": "^9.2.2",
    "standard": "^16.0.3",
    "typescript": "^4.1.3",
    "typings-tester": "^0.3.2"
  },
  "standard": {
    "ignore": [
      "**/__tests__/*.js"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{json,js,ts}": [
      "npm run test:lint",
      "git add"
    ]
  }
}
